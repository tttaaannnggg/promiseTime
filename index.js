const {isPromise} = require('./isPromise.js');
const {timeResolution} = require('./timeResolution.js');
const {addTimer} = require('./addTimer.js');
//exports utils from the file

module.exports = {isPromise, timeResolution, addTimer};
